using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BgSize : MonoBehaviour
{  
    void Awake()
    {
     //   GetComponent<RectTransform>().localScale = Vector2.one * 2/(Screen.width * 0.0018f);
    }
}
